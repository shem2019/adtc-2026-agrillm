# Superseded

The batch-paste validation prompt has been replaced by an agent brief that both
audits and expands the corpus, iterating over `train/african/` topic by topic.

See **[corpus_agent_brief.md](corpus_agent_brief.md)**.
